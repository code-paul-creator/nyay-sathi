/* Runtime config. Leave the key empty in the repository.
   The GitHub Actions workflow overwrites this file at deploy time if you add a
   repository secret named GEMINI_API_KEY. Visitors can also paste their own key in Settings. */
window.NYAY_CONFIG = { geminiApiKey: "" };
